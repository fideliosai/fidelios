# v0.0.27

> Released: 2026-04-06

## Fixes

- **Gemini adapter missing from dropdown** — `gemini_local` was in `ENABLED_ADAPTER_TYPES` (UI) but missing from `AGENT_ADAPTER_TYPES` (shared constants master list). Agents marked IRO-394/IRO-397 as done but the fix was incomplete — only half the change was made. Now properly added to the shared constants, so Gemini (local) appears in the adapter type dropdown for both create and edit views.

## Governance

- **Agent safety rules** — All 26 agent instruction files updated with: strict boundaries (no creating companies/agents), escalation rules (must create Approval request, not just comment), and test isolation (never test against production DB).
- **Worktree config guard strengthened** — Both config.json AND .env are now protected from worktree overwrite when running in default instance context.
