# v0.0.30

> Released: 2026-04-17

## New

- **`fidelios service dev` / `fidelios service release`** — flip the installed
  background service between dev mode (runs `node <repo>/scripts/dev-runner.mjs
  watch` from the FideliOS monorepo) and release mode (the published `fidelios
  run` binary from `$HOME`). Dev mode honours the
  `autoRestartDevServerWhenIdle` toggle in Settings → Experimental, so agents
  in Company `FideliOS` can edit FideliOS source without crashing their own
  in-flight runs. Release mode is the previous default behaviour.
- **`fidelios service install --dev` / `--release`** — choose the mode at
  install time. `--repo <path>` overrides auto-detection of the monorepo root.
- **`fidelios service switch <dev|release>`** — same mechanic, explicit verb.
- State is persisted in `~/.fidelios/instances/default/service-mode.json` so
  `fidelios service status` now shows `Mode: dev (hot-reload)` or
  `Mode: release` plus the dev-mode repo path.

## Docs

- [docs/cli/service-commands.md](https://docs.fidelios.nl/cli/service-commands)
  documents the mode switch, the Release-vs-Dev table, and the mode-state file.
- [docs/adapters/claude-local.md](https://docs.fidelios.nl/adapters/claude-local)
  gained a **Supported models** section listing Claude Opus 4.7, Opus 4.6,
  Sonnet 4.6, and Haiku 4.6. Closes FID-7.

## Tests

- 10 new unit tests for the dev/release plist + systemd generators and the
  `resolveDevRepoDir` helper.
- 9 new unit tests for the telegram gateway routing (precedence of UI-saved
  topics over config JSON, per-company isolation, default fallback,
  malformed-JSON handling). Closes FID-1. The plugin is now part of the
  vitest workspace.
